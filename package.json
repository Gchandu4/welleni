{
  "name": "welleni-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.3.3",
    "wrangler": "^4.0.0"
  }
}
